apiVersion: v1
kind: Secret
metadata:
  namespace: vault-admin
  name: vault-admin-default-token
  annotations:
    kubernetes.io/service-account.name: default
type: kubernetes.io/service-account-token

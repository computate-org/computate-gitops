apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-database-setup-playbook
  namespace: keycloak
data:
  keycloak-database-setup.yaml: |
    ---
    - name: keycloak-database-setup
      hosts: localhost
      connection: local
      gather_facts: false
      roles:
        - keycloak-database-setup

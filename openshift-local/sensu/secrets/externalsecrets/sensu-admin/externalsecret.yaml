apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: sensu-admin
  namespace: sensu
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: secret-store
    kind: SecretStore
  target:
    name: sensu-admin
  dataFrom:
    - extract:
        key: sensu/kv/sensu-admin
